# Family — what it cost

GENERATED by `tools/effort.py`. Do not edit: the declared half lives in
`docs/effort/declared.csv` and is the only part written by hand.

Two sources, kept apart. **Derived** is exact about what happened inside Claude Code
and blind to everything outside it. **Declared** is what a person remembers of the
rest. They are not added together into one figure, because half of that figure would
be a measurement and half a recollection.

## Derived, from the transcripts

Every gap between consecutive messages is attributed to whoever produced the message
that *ended* it: a gap closing on a reply is generation, one closing on a tool result
is a command running, one closing on your message is you reading and typing.

| idle threshold | you | Claude generating | tools running | total |
|---|---|---|---|---|
| 2 min | 1h 59m | 13h 52m | 1h 54m | 17h 47m |
| 5 min | 6h 25m | 14h 04m | 2h 41m | 23h 12m |
| 10 min | 10h 39m | 14h 04m | 2h 53m | 27h 38m |

**Claude's own time is the trustworthy row and yours is not.** Generation barely
moves with the threshold, because a reply's parts follow each other in seconds.
Yours swings several-fold, because your gaps are genuinely everything from a one-word
answer to twenty minutes in a bank. For human time the threshold is not a parameter,
it is the answer — which is why the declared ledger below exists at all.

| tokens | |
|---|---|
| fresh input | 31,599 |
| cache writes | 30,214,866 |
| cache reads | 2,162,187,258 |
| output | 7,670,902 |

Cache reads dominate and mean less than they look: every request re-sends the whole
conversation, so the same context is read back thousands of times and is priced for
that. The work done is the output and the cache writes.

11 sessions, 08 Aug 2026 to 29 Aug 2026, 344 prompts from you.

## Declared, by hand

Nothing declared yet. `docs/effort/declared.csv` takes a row per stretch of work
the transcripts cannot see — testing in the game above all.

