name: a blip with no profession line is scored against ores before herbs are matched exactly
file: addons/Family_UI/Tooltip.lua
--- old
		local one = herbs and herbNamed(name, herbs) or nil
--- new
		local one = ores and oreScored(name, ores) or nil
