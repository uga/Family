name: the possessions block offers the item gesture on a node as it does on an item
file: addons/Family_UI/Tooltip.lua
--- old
		and not ownerKeepsModifiers(tooltip)
		and not node
--- new
		and not ownerKeepsModifiers(tooltip)
