name: the loaded-list tally counts a bid-only auction as having a price
file: addons/Family/Scanners/Auctions.lua
--- old
			if each and not tally.pricedSeen[itemID] then
--- new
			if not tally.pricedSeen[itemID] then
