name: a price banned in its market still values what a member holds
file: addons/Family/Index.lua
--- old
	if type(held) == "table" and not (pair.mineBans and pair.mineBans[variant] ~= nil) then
--- new
	if type(held) == "table" then
