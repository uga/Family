name: a cursor over several different nodes is answered about whichever name comes first
file: addons/Family_UI/Tooltip.lua
--- old
				if answer == nil then
					answer = one
				elseif one ~= answer then
--- new
				if answer == nil or answer == false then
					answer = one
				elseif one ~= answer and one ~= false then
