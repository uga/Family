name: the total waits for the mailbox to close even when there is nothing left to take
file: addons/Family_UI/MailReport.lua
--- old
	if not anything and waiting <= 0 and visitMoney > 0 then sayTheTotal(true) end
--- new
	local _ = anything
