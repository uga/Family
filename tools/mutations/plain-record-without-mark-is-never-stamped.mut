name: a part no write of this version has marked is never marked when the mark is asked for
file: addons/Family/Database.lua
--- old
		if parts[part] == nil then parts[part] = Family.Codec:StableFingerprint(value) end
--- new
		if false then parts[part] = Family.Codec:StableFingerprint(value) end
