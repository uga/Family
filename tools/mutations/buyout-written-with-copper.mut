name: a buyout is written with its copper and is cut off in its column
file: addons/Family_UI/Summary.lua
--- old
	local _, _, buyout = auctionTotals(meta, key)
	return UI:GoldAndSilver(buyout or 0)
--- new
	local _, _, buyout = auctionTotals(meta, key)
	return UI:Money(buyout or 0)
