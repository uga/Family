name: any frame carrying a number is read as one of the client's own browse buttons
file: addons/Family_UI/Tooltip.lua
--- old
	if _G["BrowseButton" .. slot] ~= frame then return nil end
--- new
	if false then return nil end
