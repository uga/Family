name: the tooltip says what the bar shows money on in two modes and says nothing in the whole-family one
file: addons/Family_UI/Broker.lua
--- old
	tooltip:AddLine(" ")
	tooltip:AddDoubleLine(shows, on)
--- new
	if scope ~= "all" then
		tooltip:AddLine(" ")
		tooltip:AddDoubleLine(shows, on)
	end
