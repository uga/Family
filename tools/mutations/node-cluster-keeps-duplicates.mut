name: a node the cursor holds twice is answered about twice
file: addons/Family_UI/Tooltip.lua
--- old
		if one and not seen[one] then
			seen[one] = true
			ids[#ids + 1] = one
		end
--- new
		if one then
			seen[one] = true
			ids[#ids + 1] = one
		end
