name: the profession line has to be the word, so a character who cannot gather the node sees nothing
file: addons/Family_UI/Tooltip.lua
--- old
				if lowered:find(entry.word, 1, true) then return entry.skill, text end
--- new
				if lowered == entry.word then return entry.skill, text end
