name: a short screen gives a tooltip block fewer names than folding on would
file: addons/Family_UI/Window.lua
--- old
	return math.max(cap, math.floor(left * BLOCK_SHARE))
--- new
	return math.floor(left * BLOCK_SHARE)
