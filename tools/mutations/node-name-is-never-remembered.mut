name: a node name is worked out on every hover, naming every candidate the build ships each time
file: addons/Family_UI/Tooltip.lua
--- old
	local held = resolvedNames[said]
	if held ~= nil then return held or nil end
--- new
	local held = nil
	if held ~= nil then return held or nil end
