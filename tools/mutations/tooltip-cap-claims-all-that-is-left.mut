name: a block claims every line left, leaving nothing for an addon that draws after us
file: addons/Family_UI/Window.lua
--- old
	return math.max(cap, math.floor(left * BLOCK_SHARE))
--- new
	return math.max(cap, left)
