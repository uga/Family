name: a record this version has never written loses the mark it came with (recorded under the name of the 74 nonce case it replaced)
file: addons/Family/Database.lua
--- old
	if entry.partMarks == nil and type(entry.mark) == "string" then return entry.mark end
--- new
