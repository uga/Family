name: a clicked letter's money is read from the wrong return of its header
file: addons/Family_UI/MailReport.lua
--- old
		local _, _, _, _, letterMoney = Family:TryCall(GetInboxHeaderInfo, index)
		letterMoney = tonumber(letterMoney) or 0
--- new
		local _, _, _, letterMoney = Family:TryCall(GetInboxHeaderInfo, index)
		letterMoney = tonumber(letterMoney) or 0
