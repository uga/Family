name: the run never takes the machine's lock
file: tools/mutate.py
--- old
            fcntl.flock(handle, fcntl.LOCK_EX | fcntl.LOCK_NB)
        except OSError:
--- new
            pass
        except OSError:
