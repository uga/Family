name: a banned row is drawn like any other
file: addons/Family_UI/PriceAudit.lua
--- old
				row.alert:SetShown(entry.banned ~= nil or entry.suspect ~= nil)
--- new
				row.alert:SetShown(entry.suspect ~= nil)
