name: a record stored plain answers no mark
file: addons/Family/Database.lua
--- old
	if type(entry.payload) == "table" then return composeMark(entry) end
--- new
	if type(entry.payload) == "table" then return nil end
