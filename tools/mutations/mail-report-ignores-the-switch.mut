name: the mailbox is narrated whether or not anybody asked for it
file: addons/Family_UI/MailReport.lua
--- old
		if not wanted() then
			heldMoney, waiting = nil, 0
			return
		end
--- new
		if false then
			heldMoney, waiting = nil, 0
			return
		end
