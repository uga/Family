name: a herb node's name is matched the way a search box matches, so Silverleaf finds a pendant
file: addons/Family_UI/Tooltip.lua
--- old
		if name:lower() == lowered then return id end
--- new
		if name:lower():find(lowered, 1, true) then return id end
