name: the click is offered on an action bar slot, where it never reaches Family
file: addons/Family_UI/Tooltip.lua
--- old
	local offers = UI.ItemClickArmed and UI:ItemClickArmed()
		and not ownerKeepsModifiers(tooltip)
--- new
	local offers = UI.ItemClickArmed and UI:ItemClickArmed()
