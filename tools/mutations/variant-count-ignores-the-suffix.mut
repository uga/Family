name: the browse list is filed and counted under the base item
file: addons/Family/Scanners/Auctions.lua
--- old
		local variant = itemID and Family:VariantKey(itemID, link) or nil
--- new
		local variant = itemID
