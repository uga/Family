name: the click is offered on a client where the hook was never installed
file: addons/Family_UI/Tooltip.lua
--- old
	local offers = UI.ItemClickArmed and UI:ItemClickArmed()
		and not ownerKeepsModifiers(tooltip)
--- new
	local offers = not ownerKeepsModifiers(tooltip)
