# Libraries fetched by tools/FetchLibs.sh so that a developer's install matches a released
# one. .pkgmeta fetches its own copies at package time, so these are for testing only and are
# somebody else's code besides - neither is a reason for them to be in our history.
addons/Family/Libs/

# Caches written by the data generators
tools/.itemsources-cache/
tools/.craftlevels-cache/
tools/.skill-lines-cache/
tools/.races-cache/
tools/.areas-cache/
tools/.talents-cache/
tools/.game-words-cache/
tools/.charged-items-cache/
tools/.world-buffs-cache/
tools/.specialisations-cache/

# Bytecode left behind when one generator imports another
tools/__pycache__/

# Generated by tools/GenerateCraftLevels.py. Generated data is never committed, only
# regenerated - docs/HANDOFF.md §3 - so the file is produced into the addon and ignored here.
addons/Family/Data/

# How this repository is worked on, which is nobody else's business and was never meant to be
# published. `.claude/` was tracked on purpose so the clean-room guardrails travelled with a
# clone - and those guardrails name the tree they exist to keep sessions out of, so tracking
# them put that name in a public repository by the same door the sanitisation had just closed
# on the documents. The rule is kept by the copy on this machine, where it does the work.
CLAUDE.md
.claude/

# What this project cost, in time and tokens. Derived from Claude Code's own transcripts and
# from what Alberto declares of the hours spent in the game. It is nobody else's business and
# was never meant to be published - the same rule CLAUDE.md is kept by, and for the same
# reason. The files stay on this machine and out of every clone.
docs/EFFORT.md
docs/effort/
tools/effort.py
